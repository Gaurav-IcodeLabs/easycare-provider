import {colors} from '../constants';
const commonShadow = {
  shadowColor: colors.black,
  shadowOffset: {
    width: 2,
    height: 4,
  },
  shadowOpacity: 0.25,
  shadowRadius: 4,
  elevation: 5,
};

export {commonShadow};
